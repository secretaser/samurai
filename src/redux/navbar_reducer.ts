const initialState = {}
type initialStateType = typeof initialState

const navbar_reducer = (state = initialState, action: any): initialStateType => {
   return state
}

export default navbar_reducer;